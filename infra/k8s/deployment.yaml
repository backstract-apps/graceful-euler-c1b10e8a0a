---
apiVersion: v1
kind: Secret
metadata:
  name: graceful-euler-c1b10e8a0a-bst-token
  namespace: apps-release
  labels:
    app: graceful-euler-c1b10e8a0a
    managed-by: backstract
  annotations:
    backstract.io/token-scope: "storage.*,cache.*"
type: Opaque
stringData:
  token: "PLACEHOLDER_BST_TOKEN_REPLACE_AT_RUNTIME"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: graceful-euler-c1b10e8a0a-depl
  namespace: apps-release
  labels:
    app: graceful-euler-c1b10e8a0a
    release: prometheus # Added label for PodMonitor discovery
spec:
  replicas: 0
  selector:
    matchLabels:
      app: graceful-euler-c1b10e8a0a
      release: prometheus # Added label for PodMonitor discovery
  template:
    metadata:
      labels:
        app: graceful-euler-c1b10e8a0a
        release: prometheus # Added label for PodMonitor discovery
    spec:
      serviceAccountName: alloy-service-account
      volumes:
      - name: alloy-config
        configMap:
          name: alloy-configmap
          items:
          - key: alloy.river
            path: config.alloy

      - name: podinfo
        downwardAPI:
          items:
          - path: "labels"
            fieldRef:
              fieldPath: metadata.labels
          - path: "annotations"
            fieldRef:
              fieldPath: metadata.annotations
      tolerations:
        - key: "workload"
          operator: "Equal"
          value: "user"
          effect: "NoSchedule"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodepool
                    operator: In
                    values: ["spot-users"]
      containers:
      - name: graceful-euler-c1b10e8a0a
        image: 216084506783.dkr.ecr.ap-south-1.amazonaws.com/backstract_apps:graceful-euler-c1b10e8a0a
        imagePullPolicy: Always
        env:
        - name: BACKSTRACT_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: graceful-euler-c1b10e8a0a-bst-token
              key: token
        - name: BACKSTRACT_PLATFORM_URL
          value: "https://api-release.beemerbenzbentley.site/sigma"
        envFrom:
        - configMapRef:
            name: env-configmap-coll-092b8b35e08a4aaab30bb53e6d6a2b78-deployment-143bfedd-fa59-481c-872a-3f4b5f3c8c74
        - secretRef:
            name: app-secrets-coll-092b8b35e08a4aaab30bb53e6d6a2b78
        ports:
        - containerPort: 7070
          name: http # Optional for HTTP traffic
        - containerPort: 7070 #same as the deployed code
          name: metrics # Added port for metrics scraping
        resources:
          requests:
            memory: "210Mi"
            cpu: "50m"
          limits:
            memory: "500Mi"
            cpu: "300m"

      - name: alloy-sidecar
        image: grafana/alloy:latest
        args:
        - "run"
        - "/etc/alloy/config.alloy"
        - "--server.http.listen-addr=0.0.0.0:12345"
        ports:
        - name: otlp-grpc
          containerPort: 4317
          protocol: TCP
        - name: otlp-http
          containerPort: 4318
          protocol: TCP
        - name: alloy-http
          containerPort: 12345
          protocol: TCP
        volumeMounts:
        - name: alloy-config
          mountPath: /etc/alloy
        - name: podinfo
          mountPath: /etc/podinfo
          readOnly: true
        resources:
          requests:
            memory: "128Mi"
            cpu: "20m"
          limits:
            memory: "384Mi"
            cpu: "100m"